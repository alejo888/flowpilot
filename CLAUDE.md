# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

FlowPilot — an AI-assisted agile project management platform intended as a personal portfolio project. Full product vision lives in `FlowPilot_Gestor_Proyectos_IA.md`; that document is broader than the current MVP. Backend: Spring Boot 4 (Java 25). Frontend: Angular 21 (standalone components). Database: PostgreSQL via Flyway. Shipped as Docker Compose services.

## Current MVP status

Implemented and covered by backend/frontend tests at a high level:

- Auth/session backend: register, login, refresh-token rotation, logout, forgot-password, reset-password.
- Auth/session frontend: login/register/forgot-password/reset-password screens, refresh-cookie hydration, JWT interceptor, route guards, admin guard.
- User directory backend: authenticated user list and user detail.
- Admin users: admin-only user list, activation/deactivation, global role changes, last-active-admin guard; frontend screen is wired.
- Projects: backend list/create/get/update/status/delete with rich fields and default board columns; frontend supports list/create/detail navigation, edit, status change, and delete with accessible confirmation, plus links to board/members. `/projects/:projectId` renders a project-detail screen with embedded comments and activity feed sections. `GET`/create/update responses also carry `callerPermissions` — the requesting caller's own matrix-backed permission set for that project (`ProjectAuthorizationService.permissionsFor`) — so the frontend can disable a control the caller can't use instead of only discovering a 403 on submit; `project-detail`, `project-members`, `board`, and `backlog` all gate their destructive/mutating controls this way (fail-closed while the project hasn't loaded yet).
- Project members: backend and frontend list/add/change role/remove, including self-removal confirmation. Self-removal requires `MEMBER_REMOVE` the same as removing anyone else (no special case).
- Work items / Kanban: backend CRUD plus move between/within columns; frontend board lists cards, supports drag/drop move, and create/edit/delete via an overlay detail panel. Drag itself is disabled (not just the drop handler) when the caller lacks `WORKITEM_MOVE`.
- Backlog and sprints: project backlog browsing and sprint planning with work-item assignment.
- Role-permission matrix: backend dense 6x11 role/permission seed (includes `SPRINT_MANAGE`, `COMMENT_CREATE` alongside the original catalog), admin read/update, optimistic concurrency (row-locked bulk replace, `OffsetDateTime.isEqual()` for the stale-timestamp guard so a same-instant-different-offset timestamp never causes a spurious 409), authorization cache reload deferred to post-commit; frontend matrix screen is wired.
- Own profile: backend `GET /api/users/me` and `PUT /api/users/me/password` (verifies the current password, re-hashes, revokes all of the caller's active refresh tokens — mirrors `PasswordResetService`); frontend `/profile` screen shows name/email and a change-password form, reachable from a "Perfil" nav link for any authenticated user.
- Dashboard/metrics: backend `GET /api/projects/{projectId}/dashboard` (`ProjectDashboardService`) returns per-column counts, sprint summary, and per-assignee workload; frontend `/projects/:projectId/dashboard` renders totals, backlog pending count, per-column flow, priority distribution, active sprint progress, and assignee workload.
- Comments and project activity feed: backend supports project-scoped and work-item-scoped comments (create/read/update/delete, delete and edit both author-only *and* requiring the caller still hold `COMMENT_CREATE` — a role that's had the permission revoked can no longer edit its own past comments either) plus an authorization-checked project activity feed (`CommentController`, `ProjectActivityService`), both query-paginated (`Pageable`, not in-memory skip/limit) and both explicitly 404 a nonexistent project regardless of caller role (closes a gap where a global admin's `canView` short-circuit skipped the existence check). All backend-authored text — denial messages, validation messages, and every activity event's `displayText` across `CommentService`/`WorkItemService`/`ProjectService`/`BoardService`/`SprintService` — is Spanish, naming the affected entity where cheaply available (e.g. `Se creó la tarea "X"`). Frontend renders comments/activity in the project-detail screen's comments and activity sections, plus in the board's work-item detail panel; a failed create/edit/delete never silently discards what the user typed (`CommentsStore`'s write methods return `Promise<boolean>`, resolving `true` only on confirmed success — local UI state is cleared only then), the delete-confirmation dialog surfaces its error from inside the dialog panel (not behind the backdrop), editing a comment keeps its list position instead of jumping to the top, and navigating between projects/work-items clears stale comments/activity before the new fetch instead of briefly showing the previous context's data. Each comment's author/date line and its Editar/Eliminar action row are separate flex rows (`.comment-meta`, `.comment-actions`) inside a `display:flex; flex-direction:column` article — needed because the article previously had no layout of its own, so the author/date text ran together and, while editing, the textarea visually overlapped the line above it.
- API contract: `api/openapi.yaml` covers the implemented path families at a high level, including documented 4xx `ProblemDetail` responses and `security: bearerAuth` on every comment/activity operation, and a full `enum:` on `ActivityResponse.eventType` (was previously a bare `type: string` despite Springdoc emitting the enum).
- Demo data: `V9__seed_demo_data.sql` seeds 4 demo users (`Demo1234!`) plus 3 projects with members and work items, so a fresh `docker compose up --build` isn't empty. Root `README.md` has screenshots, the demo credential table, and a feature tour.
- **Judgment Day adversarial review (2026-08-23 through 2026-08-26)**: all 10 feature modules (Auth/Session, Admin Users, Role-Permission Matrix, Projects, Project Members, Work Items/Kanban, Backlog/Sprints, Dashboard, Own Profile, Comments/Activity Feed) went through dual-blind adversarial review (two independent reviewer passes per module, confirmed findings fixed, re-reviewed) — every module reached a clean approved state, plus a follow-up backlog cleanup pass (comment/activity behavior bugs, Spanish-language coverage, OpenAPI contract drift, technical debt, and the caller-permission-gating contract change above). All fixes are merged to `main`.

Pending or partial for portfolio readiness:

- AI-assisted planning features — entirely unimplemented; no AI/LLM integration exists anywhere in the codebase yet.
- E2E coverage (`frontend/e2e/`) now also runs an axe-core WCAG 2.1 A/AA accessibility check (`accessibility.*.spec.ts`, `assert-no-a11y-violations.ts`) across every authenticated static route including `/projects/:id/dashboard` and `/projects/:id/backlog`, plus board/members, and the guest-only specs (no login required) run cross-browser in `firefox-guest`/`webkit-guest` Playwright projects in addition to the existing chromium ones. The authenticated (`*.authenticated.spec.ts`) and desktop (`*.desktop.spec.ts`) specs now also run on Firefox (`firefox-authenticated`/`firefox-desktop`), so the responsive/a11y sweep covers a second rendering engine on the authenticated routes. The refresh-token reuse-detection concurrency issue is handled, not avoided: `global-setup` captures a separate admin session per engine (`admin.json`/`admin-firefox.json`) so no engine ever replays another's single-use, already-rotated refresh cookie, and both Firefox projects are chained via `dependencies` back through the chromium chain so no two authenticated runs are ever in flight at once (a rotation collision inside one admin session triggers the backend's revoke-ALL-sessions response, which would otherwise cascade across engines). WebKit stays guest-only for the authenticated matrix: its network stack drops the `Secure` refresh cookie over the plaintext `http://localhost` e2e origin (Chromium/Firefox treat localhost as a secure context; WebKit does not), so a restored WebKit session is silently unauthenticated and every authenticated route redirects to `/login` — covering it needs an HTTPS e2e origin, not another Playwright project. This is still a serialized sweep of the static/responsive routes, not a cross-browser run of every interactive flow.
- A full manual + automated responsive/a11y sweep (2026-08-22) at 375px found and fixed three real issues beyond the comment-layout bug: (1) `.board-header`/`.detail-header` (`board.component.scss`) had no `flex-wrap`, so on narrow viewports the "Crear tarea" button ran off the right edge of the page instead of wrapping — the existing e2e "board" overflow test hadn't caught it because the seeded test project's short content happened to fit; (2) `--fp-accent` (`styles.scss`) still failed WCAG AA (4.45:1, needed 4.5:1) against `--fp-bg` (`#faf7f2`, the actual page background text like `.eyebrow` sits on) even after the first fix in this file, which was only checked against pure white — now `#bd4c24` (4.97:1 vs white, 4.65:1 vs `--fp-bg`); (3) the admin permission matrix's per-cell checkboxes (`admin-permissions.component.ts`) had no accessible name (`aria-label="{{role}}: {{permission.label}}"` added) — a WCAG 4.1.2 critical violation only surfaced once `/admin/permissions` got real a11y coverage. All other routes (home, projects list/detail, board incl. work-item detail panel and comment edit/delete, members, profile, admin users, admin permissions, login/register/forgot-password/reset-password) were verified overflow-clean at 375px against real seeded content, not just the e2e fixture project.
- Error/success message translation to Spanish now covers auth (login, register, forgot-password, reset-password) plus projects, project members, work-items/board, admin (users, role-permissions), profile/change-password, refresh-token/logout error details (`RefreshTokenService`), the CSRF-filter error, the `CommentController` pagination constraints (`@Min`/`@Max` on `limit`/`offset`) that previously fell through to Jakarta's default English validation messages via `GlobalExceptionHandler.handleConstraintViolation`, comment create/edit denial messages and validation messages, and every project-activity `displayText` across all five services that record activity events.
Frontend validation: 413 tests pass. Backend: 339 tests pass. The production build passes cleanly. `frontend/angular.json`'s `anyComponentStyle` budget is `maximumWarning: 5kB` / `maximumError: 8kB` (raised from the CLI's 4kB default) — `board.component.ts`'s styles organically grew past 4kB and had no dead CSS left to trim (verified every selector is referenced in the template) when the comment layout fix above was added.

Judgment Day's backlog is now fully closed, including the two remaining Judge-B-only role-permission-matrix items: `RolePermissionAdminService.replaceAll` only stamps `updatedAt`/`updatedBy` when a grant actually flips value (previously every resubmitted row got a fresh timestamp, even unchanged ones, since the client always resubmits the full dense matrix), and `RolePermissionsStore.toggle()` now no-ops while a save is in flight (matching the Save button's own `saving()`-gated disable, extended to every grid cell) instead of letting a mid-save edit get silently overwritten when the response lands.

## Commands

Backend (from `backend/`):
- `./mvnw -B test` — full test suite (what CI runs)
- `./mvnw test -Dtest=ClassName` — single test class
- `./mvnw spring-boot:run` — run the app locally
- No `.mvn/jvm.config` pins the JDK. If your machine's default `JAVA_HOME` resolves to an older JDK (e.g. 17), export a JDK 25 `JAVA_HOME` before invoking `./mvnw` — there is no repo-level workaround yet.
- Integration tests use Testcontainers 1.21.2 and require Docker. The full suite includes `FlowpilotApplicationTests`, `AuthFlowIntegrationTest`, `OpenApiSpecExportTest`, and `RolePermissionSeedIntegrationTest`; they won't run in a Docker-less sandbox. Exclude them with `-Dtest='!FlowpilotApplicationTests,!AuthFlowIntegrationTest,!OpenApiSpecExportTest,!RolePermissionSeedIntegrationTest'` when Docker isn't available.
- If Testcontainers cannot discover Docker on a Windows developer machine, use the local configuration files `~/.testcontainers.properties` (with `docker.host`) and `~/.docker-java.properties` (with `api.version`) as troubleshooting overrides. Create them only when discovery fails; do not commit machine-specific named-pipe or API-version settings to the repository.

Frontend (from `frontend/`):
- `npm ci`
- `ng test --watch=false` — unit tests (Vitest under the hood, not Karma/Jasmine, despite the `ng test` invocation)
- `ng build`
- `ng serve` — dev server

Full stack: `docker compose up --build` — backend on `:8080` (proxied under `/api`), frontend/nginx on `:80`, Postgres on `:5432`.

## Architecture

**Backend package layout** (`backend/src/main/java/com/flowpilot/`): `config`, `controller`, `dto`, `entity`, `exception`, `mapper`, `repository`, `security`, `service` — layered, not hexagonal. MapStruct is wired into the Maven compiler plugin; mapper/config packages currently remain light.

**Auth flow**: Login authenticates via Spring's `AuthenticationManager`, then issues two artifacts: a short-lived (15 min) stateless HS256 JWT access token in the response body (`JwtService`, jjwt 0.12.6), and an opaque, DB-backed refresh token (7-day TTL) as an `HttpOnly; Secure; SameSite=Lax` cookie scoped to `/api/auth` (`CookieService` + `RefreshTokenService`) — only the SHA-256 hash is persisted. `JwtAuthenticationFilter` validates the bearer token on every non-`/api/auth/**` request without touching the DB — a deliberate MVP tradeoff meaning a deactivated user's already-issued access token stays valid until it naturally expires. Because the refresh cookie rides automatically on cross-site requests, `/api/auth/refresh` and `/api/auth/logout` are protected by a custom double-submit CSRF filter (`CsrfProtectionFilter`) instead of Spring Security's built-in CSRF, which is fully disabled: a non-HttpOnly `XSRF-TOKEN` cookie is set alongside the refresh cookie and must be echoed back as an `X-XSRF-TOKEN` header. `RefreshTokenService.rotate()` does rotation-with-reuse-detection: presenting an already-revoked refresh token revokes *all* of that user's active refresh tokens. All errors funnel through `GlobalExceptionHandler` as RFC 7807 `ProblemDetail` responses. Password reset token delivery is still MVP-grade; wire real email delivery before production use.

**Domain slices**: Project/member/work-item writes are guarded by project role permissions through `ProjectAuthorizationService`. Its `hasPermission(userId, projectId, permission)` is the single-permission check writes call; `permissionsFor(userId, projectId)` computes the caller's *entire* permission set for a project in one pass (same deactivated/admin/owner/member-role lookup chain, without repeating it once per permission) — used to populate `ProjectResponse.callerPermissions` for frontend control-gating. Admin screens use separate admin-only endpoints for global user management and role-permission matrix management. Work item move uses board-column validation and position calculation so cards can move across columns and within a column.

**Migrations** (`backend/src/main/resources/db/migration/`):
- `V1__users_and_auth.sql` — users, refresh tokens, password reset tokens.
- `V2__seed_admin.sql` — bootstrap admin `admin@flowpilot.local` / `ChangeMe123!` for local/dev only; rotate in real environments.
- `V3__projects_and_columns.sql` — projects and default board columns.
- `V4__project_members.sql` — project membership.
- `V5__work_items.sql` — work items.
- `V6__role_permissions.sql` and `V7__seed_role_permissions.sql` — project role-permission matrix.
- `V8__project_rich_fields.sql` — project code, dates, technologies, repository URL, and related fields.
- `V9__seed_demo_data.sql` — demo users and projects (see MVP status above).
- `V10__sprints_and_work_item_assignment.sql` — `sprints` table (name, goal, dates, status, unique-active-per-project constraint) plus `sprint_id` FK on `work_items`.
- `V11__work_item_priority.sql` — `priority` column on `work_items` (LOW/MEDIUM/HIGH/URGENT, default MEDIUM).
- `V12__comments_and_project_activity.sql` — `comments` table (project- or work-item-scoped, author, content up to 4000 chars) and `project_activity` table (actor, event type, display text, JSON payload).
- `V13__seed_comment_permission.sql` — seeds `COMMENT_CREATE` role-permission grants for PROJECT_MANAGER, PRODUCT_OWNER, DEVELOPER, UX_UI_DESIGNER, QA, DEVOPS.
- `V14__project_optimistic_locking.sql` — `@Version` column on `projects`.
- `V15__project_member_optimistic_locking.sql` — `@Version` column on `project_members`.
- `V16__work_item_optimistic_locking.sql` — `@Version` column on `work_items`.
- `V17__seed_sprint_permission.sql` — seeds `SPRINT_MANAGE` role-permission grants (6x11 matrix).
- `V18__sprint_optimistic_locking.sql` — `@Version` column on `sprints`.
- `V19__drop_tautological_comments_constraint.sql` — drops `comments_single_target`, a CHECK constraint that could never fail (`project_id` was already `NOT NULL`, so its `OR` was always true) and falsely implied the DB enforced a comment-targeting invariant it didn't; that invariant is enforced in application code (`CommentService.createForWorkItem` derives `projectId` from the work item).

Flyway requires the `spring-boot-starter-flyway` dependency (not just `flyway-core`) to auto-run on startup in Spring Boot 4.

**Config profiles**: base `application.yml` holds shared defaults; `application-dev.yml`, `application-docker.yml` (datasource host `db`, the Compose service name), `application-prod.yml`, and test's `application-test.yml` override only what differs (datasource, JWT secret, logging). The `docker` profile has no insecure JWT-secret fallback — Compose fails fast if `FLOWPILOT_JWT_SECRET` isn't set. The `prod` profile requires `FLOWPILOT_DATASOURCE_URL`, `FLOWPILOT_DATASOURCE_USERNAME`, `FLOWPILOT_DATASOURCE_PASSWORD`, and `FLOWPILOT_JWT_SECRET` as env vars with no defaults — Spring fails fast at startup if any are missing.

**Frontend**: standalone Angular app with implemented routes for `/`, `/login`, `/register`, `/forgot-password`, `/reset-password`, `/projects`, `/projects/:projectId`, `/projects/:projectId/dashboard`, `/projects/:projectId/board`, `/projects/:projectId/backlog`, `/projects/:projectId/members`, `/profile`, `/admin/users`, and `/admin/permissions`. Core auth/API code lives under `frontend/src/app/core/`; feature slices live under `frontend/src/app/features/`; shared UI components live under `frontend/src/app/shared/ui/`. Remaining gap: AI-assisted planning has no backend capability yet, so there's no screen for it either.

**API contract**: `api/openapi.yaml` is the hand-authored, contract-first source of truth — write the spec before the controller, not the other way around. The contract is currently monolithic; `api/components/` and `api/paths/` are not populated. CI exports the live Springdoc spec during backend tests and runs a blocking `oasdiff` breaking-change report (`fail-on: ERR`) — keep the manual spec's request/response schemas (including `format`/`nullable`) aligned with what Springdoc actually generates to avoid false breaking-change failures.

**Delivery model**: one Git branch/PR per SDD task-slice, stacked toward `main` where practical. CI (`.github/workflows/ci.yml`) runs backend (`./mvnw -B test`, JDK 25) and frontend (`npm ci && ng test --watch=false && ng build`, Node 24) as separate parallel jobs on every PR and push to `main`. Caution with stacked PR chains: `gh pr merge` on a PR whose base is another feature branch (not `main`) only advances that intermediate branch — it does **not** land the change on `main` even though the PR shows as merged. Verify with `gh pr list --json baseRefName` before assuming a stacked chain has reached `main`; if a squash-merge already landed partway through a chain that was meant to be linear, recover with one regular (non-squash) `git merge` of the chain's tip into `main` rather than re-squashing each intermediate PR (squashing rewrites history and breaks fast-forwarding for everything stacked above it).

package com.flowpilot.entity;

/**
 * Global (system-wide) role assigned to a {@link User}.
 * Enum names match the values already seeded by {@code V2__seed_admin.sql}.
 */
public enum GlobalRole {
    ADMINISTRADOR,
    MIEMBRO_EQUIPO
}

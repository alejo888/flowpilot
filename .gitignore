# Backend (Maven / Java)
backend/target/
*.class
*.jar

# Frontend (Angular / Node)
frontend/node_modules/
frontend/dist/
frontend/.angular/
npm-debug.log*
/node_modules/

# IDE
.idea/
*.iml
.vscode/
.DS_Store

# Env / secrets
.env
*.env.local

# Local Compose overrides (per-developer, not vendored)
docker-compose.override.yml

# OS
Thumbs.db
nul

# Playwright MCP session artifacts (screenshots/snapshots, not part of the project)
.playwright-mcp/

# Local agent tooling (not part of the project)
.atl/

# Agent skills/tooling are installed per developer, not vendored in the portfolio repo
**/.claude/skills/
**/.agents/
**/skills-lock.json

